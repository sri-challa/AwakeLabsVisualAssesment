import { View } from "react-native";
import UsersList from "../components/UsersList";

const HomeScreen = () => {
  return (
    <View>
      <UsersList />
    </View>
  );
};

export default HomeScreen;
